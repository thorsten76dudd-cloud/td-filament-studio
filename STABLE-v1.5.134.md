# TD Filament Studio — v1.5.134-stable

**Release-Tag:** `v1.5.134-stable`

## Fixes

- In-App-Update: Installer per ShellExecute (sichtbar), nicht mehr per wscript/unsichtbarem Start.
- App beendet sich nach OK zuverlässig (`os._exit`), damit Setup laufen kann.
